{
  "version": "0.7.0",
  "date": "2026-03-06",
  "notes": "- NSIS installer: custom branded sidebar image (164×314 px) and header image (150×57 px)\r\n- NSIS installer: optional desktop shortcut — checkbox page during installation (checked by default)\r\n- Wizard: new License step (step 7/8) — choose between 14-day free trial or activate a Pro key directly during first-launch setup\r\n- License manager: fully responsive layout — sidebar collapses on small screens with a `<` toggle button fixed at bottom-right\r\n- License manager: date picker with enforced minimum — expiry date cannot be before or equal to start date, updates dynamically when start date changes\r\n- License manager: duplicate email protection — server blocks generation if a license already exists for the same email (HTTP 409)\r\n- License manager: Delete button per license in History — removes from local log and revokes activation on Cloudflare Worker\r\n- License manager: History auto-refreshes after each successful key generation (no manual tab switch needed)\r\n- License activation modal: clicking Next no longer closed the modal — modal now stays open on validation error (keepOpen flag)\r\n- License activation modal: errors now display as inline red messages below each field instead of a single bottom message\r\n- License manager: expiry date field min enforced as start + 1 day (previously allowed same-day expiry)\r\n- NSIS installer: previous version is automatically uninstalled before installing the new one — no more duplicate entries in Windows Programs & Features\r\n- NSIS installer: desktop shortcut creation moved from automatic to user-controlled (opt-in checkbox)\r\n- Version bumped to 0.7.0\r\n- `build/installer.nsh`: `customInit` macro reads `HKLM` and `HKCU` registry to detect and silently uninstall previous version before install\r\n- `build/installer.nsh`: `DesktopShortcutPage` / `DesktopShortcutPageLeave` functions handle checkbox state for desktop shortcut\r\n- `package.json`: `installerSidebar`, `uninstallerSidebar`, `installerHeader` fields added pointing to `assets/*.bmp`\r\n- `package.json`: `createDesktopShortcut: false` — shortcut creation delegated to `installer.nsh`\r\n- `server.js`: duplicate email check against `licenses.log` before key generation\r\n- `ui.html`: `loadHistory()` called automatically after successful key generation\r\n- `ui.html`: `FORMAT_WORKER_URL` + `FORMAT_ADMIN_SECRET` read from `.env` via `dotenv` for Cloudflare revocation on delete",
  "download": "https://github.com/djilanrene/format-updates/releases/download/v0.7.0/format-setup-v0.7.0.exe"
}